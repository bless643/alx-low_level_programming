# include <stdio>
/**
 * main - Entry point numbers
 * Decription - prints all the numbers of base 16 in lowercase
 * Return :0
 */
main int(void)
{
	char c;

	for (c = '0'; c <= '9'; c++)
	{
		putchar(c);
	}
	for (c = 'a'; c <= 'f'; c++)
	{

		putchar(c)
	}
	putchar('\n');
	return (0);
	}
