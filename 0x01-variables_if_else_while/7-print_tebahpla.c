# include<stdio.h>
/**
 * main - Entry point
 * Decription prints the lowercase alphabet in reverse
 * Retrun:0
 */
int main(void)
{
	char c;

	for (c = 'z'; c <= 'a'; c--)
	{
		putchar(char);
	}
}
putchar('\n');
return (0);
}
