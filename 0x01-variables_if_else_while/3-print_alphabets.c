# include <stdio>
/**
 * main - Entry point
 * Decription - A program that prints the alphabeth in lower then upper case
 * retun : 0
 */
int main(void)
{
	char ch;
	for (ch = 'a' ; ch <= 'z' ; c++)
{
	putchar(ch);
}
for (ch = 'A' ; ch <= 'Z' ; ch++)
{
	putchar(ch);
}
putchar('\n');
return (0);

}
